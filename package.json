{
  "name": "petrel-pioneers",
  "private": true,
  "version": "0.2.0",
  "type": "module",
  "description": "Petrel Pioneers — core loop prototype (v0.2). Automatic-with-direction colony sim.",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "vite": "^7.3.5"
  }
}
